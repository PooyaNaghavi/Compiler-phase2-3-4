
class MainClass{
 def main(): int{
   writeln((new sag().sex2(10).sex2(10).sex1(120)));
    return 0;
 }
}
class Tataloo extends sag{

    def sex(x:int, f:string, h:int): int{
        writeln((new sag().sex1(12)));
        return 10;
    }

}
class sag {
var ff : sag;
 def sex1(x : int): int{
 var ffff : string;
 var f : int[];
 var g : string ;
        var ffffa: int;
        var fggfaa: boolean;
        var l : boolean ;
        var ffffaaa: int;
var zffffaaa: string;
        ffff = "ali0";
        l = true == false;
        x = x + 10 + 2;
          ff = new sag();

        f = new int[10];
       f[9]= 10 +  new sag().sex2(20).sex3(10) ;

         x = 1+ f[9]  ;
    writeln( f.length + x);
    writeln( 4+ new sag().sex2(20).sex3(10) + f.length + x);
       writeln(f.length);
        return x;
    }
    def sex2(x : int): sag{
 var ffff: string;
        var f : int[];
        ffff = "ali0";
        x = x + 10 + 2;
          ff = new sag();

        f = new int[10];
        f[9]= 10;
        writeln(f.length);
         x = 1+ f[9]  ;
       writeln(f.length);
            return new sag();
        }
   def sex3(x : int): int{
            return x;
        }
}